# Foundry
out/
cache/
broadcast/
!broadcast/**/*-latest.json
foundry.lock

# Gas snapshot — tracked starting todo-22 (Faz 1 closeout). The file is now
# the regression baseline; CI gate (todo-63) will run `forge snapshot --check`
# on every PR. Refresh manually with `forge snapshot` and justify any delta in
# the commit message.

# Environment
.env
.env.*
!.env.example

# IDE / OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Logs
*.log
.logs/
.ignore/

# Node (verifier-service — Faz 4)
node_modules/
verifier-service/dist/
verifier-service/.env

# Coverage
lcov.info
coverage/

# forge doc auto-generated NatSpec site (rebuild from source on demand)
docs/natspec/

# Project-specific — public repo'da tutulmaz
# research/  : paper, benchmark raw, social-evidence, ADR (lokal-only)
# .claude/   : todo roadmap, plan'lar, progress, social drafts, solutions,
#              command tanımları, kişisel ayarlar (internal workflow,
#              rakip için strategy ifşası)
# learn/     : mülakat hazırlığı + Q&A korpusu (public olunca soru bankası
#              daralır + ezber-cevap algısı yaratır)
research/
.claude/
learn/
